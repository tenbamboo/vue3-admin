import store from '@/store/'

export const useBeforeEach = (_router) => {
    _router.beforeEach(code404)

}

const code404 = (to, _, next) => {
    store.

    store.state.layout

}


export default {
    useBeforeEach
}